I used discover on a project that was on branch1. I created subproject with git init. Everything was OK but discoverXX --write which didn't write the fallback-branch as branch1 that was the branch of the split repo. the gitTree resulting from cgitsync initialise of the generated file was READY and status displayed the branch properly. But add, commit worked but not push. Actually the pb is complex cause only the main branch of the repo existed and then i was able to checkout the branch everywhere in the distant repo. I didn't find the UX fluid and those warnings if not possible to counterbalance must be documented in the detailed user-guide with a warning in the tuto. The case was molonari.cgs. Please also not that for a nested config Project-name and root-repo-name must match. I also got a bug in standalone when the two were different but i didn't confirm it was the cause of it.