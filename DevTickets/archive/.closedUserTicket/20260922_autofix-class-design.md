*Emitted by the owner in conversation on 2026-09-22, not as a file. Recorded
here verbatim, and closed the same day by the change that carried it out,
because the record of what was asked for should not live only in a chat
log.*

---

autofix.py should mobilize a class called autofix that run a check on the
situation of a git. Let start from cgitsync errors for launching the
proper sequence of actions. The repair is called by pixi run cgitsync
autofix (it takes the former error as an entry)
